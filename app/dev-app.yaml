apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mlops-app-dev
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/Shivaji1487/mlops-gitops.git'
    targetRevision: HEAD
    path: 'charts/ml-service'  # Change to 'applications/helm' if you haven't renamed it yet
    helm:
      valueFiles:
        - values-dev.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true